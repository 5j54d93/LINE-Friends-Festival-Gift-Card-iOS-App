import XCTest
@testable import LINE_Festival_Card

final class LINE_Festival_CardTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(LINE_Festival_Card().text, "Hello, World!")
    }
}
